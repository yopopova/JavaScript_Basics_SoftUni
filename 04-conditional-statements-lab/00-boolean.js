function ifs() {
    let a = 4;
    let b =7;

    let result = a < b; // true on the console; on this row we compare the two values
    
    // let result = a > b; false on the console; on this row we compare the two values
    // let result = a === b; '===' is a Boolean comparison

    console.log(result); // The result variable holds a boolean expression that is either true or false.
}

ifs();